package com.ecommerce.entity;

/**
 * Enum representing user roles in the system.
 * USER: Regular customer with shopping privileges
 * ADMIN: Administrator with full system access
 */
public enum Role {
    USER,
    ADMIN
}